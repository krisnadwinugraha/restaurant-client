import { Typography, Container } from '@mui/material';

export default function FoodMaster() {
  return (
    <Container>
      <Typography variant="h4">Restaurant Food Master</Typography>
    </Container>
  );
}